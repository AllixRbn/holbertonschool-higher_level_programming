-- Creates the table force_name in the database hbtn_0d_2 with the following columns:
-- id: INT
-- name: VARCHAR(256) NOT NULL
CREATE TABLE IF NOT EXISTS force_name (
    id INT,
    name VARCHAR(256) NOT NULL
);
